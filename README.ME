A Visual Studio project written in C#, test connection with Oracle  
by ODP.NET managed driver, which does not need to have Oracle client  
installed. Instead, simply reference "Oracle.ManagedDataAccess.dll"  
will do the work.